# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local/private planning notes. Do not publish personal account details,
# local machine paths, or unreleased business operations.
.planning/

# Playwright
/test-results/
/playwright-report/

# Rust build output
target/
src-tauri/target/

# ONNX model weights (downloaded in Phase 2+; never commit large binaries)
# ONNX: only buffalo_s weights (~5 MB total) are committed and bundled in the app.
# Older multi-hundred‑MB ONNX must never return to git — use insightface downloader if these are missing.

# Updater signing private key (pubkey stays in src-tauri/tauri.conf.json)
src-tauri/.tauri-glanceguard-updater.key

# Local signing material / downloaded Apple portal artifacts
*.certSigningRequest
*.p8
*.p12
developerID_application.cer

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
